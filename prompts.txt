PROMPT 1: BUG FIXES & PERFORMANCE OPTIMIZATION
---------------------------------------------
Identify and fix the following bugs in src/App.tsx:
1. Fix the analytics heartbeat: The current analytics interval is using an empty dependency array, meaning it never sees the updated 'agentName'. Use a ref to track the latest value or update the dependency array correctly (with cleanup).
2. Fix unnecessary re-fetching: Remove the fetchAPI() calls from 'handleLayerSelect', 'handleSkillSelect', and the profile dropdown's onChange. Data should only be fetched once on mount.
3. Fix direct state mutation: In 'handleLayerSelect', the code is pushing directly to the state array. Replace this with proper immutable state updates.
4. General code cleanup: Ensure all useEffect hooks are properly managed and that simulated API delays don't block user interactions unnecessarily.

PROMPT 2: REFACTORING & TAILWIND STYLING
----------------------------------------
Refactor the application to use Tailwind CSS and extract components:
1. Replace all inline styles in src/App.tsx with Tailwind utility classes.
2. Break down the App component into smaller, manageable components (e.g., Header, Sidebar/Options, Preview, SavedAgents).
3. Ensure the layout is responsive (e.g., stacked on mobile, side-by-side on desktop).
4. Use a clean, modern color palette (consider using Slate/Gray with Indigo/Cyan accents).

PROMPT 3: IMPLEMENT DRAG & DROP BUILDER
---------------------------------------
Implement a drag-and-drop interface for building the agent:
1. Install @dnd-kit/core and @dnd-kit/sortable.
2. Replace the select menus for 'Skills' and 'Layers' with a 'Draggable' list of available items and a 'Droppable' target area in the configuration preview.
3. Allow users to drag items from the available list into the agent's configuration.
4. Implement a way to remove items from the configuration (either by dragging them back or a delete button).
5. Add smooth transitions and visual feedback during dragging.

PROMPT 4: CV INTEGRATION & CREATIVE UI
--------------------------------------
Add the CV and final polish:
1. Place a PDF version of your CV in the 'public/' directory (you can use a placeholder for now).
2. Create a creative "Agent Profile" for yourself in the UI that displays your CV data in a fun way (e.g., "Meet the Developer" agent).
3. Add a way to download the PDF CV directly from the app.
4. Ensure the overall UX is "wow" worthy with animations (e.g., framer-motion) and polished UI elements (cards, shadows, hover effects).
5. Final validation: Ensure 'Saved Agents' works correctly with the new D&D architecture and that localStorage persistence is maintained.
